// Affichage conditionnel

// Variables de l'exercice précédent (modifie selon tes valeurs)
const nom_boutique = "L'incoyable Bric-à-brac sans broc";
const boutique_ouverte = false;

if (boutique_ouverte) {
    console.log(`Bienvenue à ${nom_boutique}!`);
} else {
    console.log(`${nom_boutique} est fermée, revenez plus tard.`)
}
